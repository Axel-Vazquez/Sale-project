# Sale-project
I carried out a sales analysis project from start to finish, simulating the typical work of an analyst. I then loaded the data into an SQL database using a star schema so I could run business queries. I then integrated all of that into a Power BI dashboard so that someone who doesn't know SQL can also view the results. 
